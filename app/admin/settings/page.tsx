import { AdminPlaceholderView } from "@/components/admin/admin-placeholder-view";

export default function AdminSettingsPage() {
  return (
    <AdminPlaceholderView
      eyebrow="PROGRAM SETTINGS"
      title="SETTINGS"
      body="Program settings and owner-side configuration are still pending. This route is now attached to the shared admin shell so the next settings pass can focus only on functionality."
    />
  );
}
