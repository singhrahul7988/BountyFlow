import { AdminPlaceholderView } from "@/components/admin/admin-placeholder-view";

export default function AdminCreatePage() {
  return (
    <AdminPlaceholderView
      eyebrow="PROGRAM CREATION"
      title="CREATE BOUNTY"
      body="Bounty creation with tier configuration, AI threshold controls, and funding flow is still pending. The route now exists inside the owner shell so the full create flow can be built next."
    />
  );
}
