import { AdminPlaceholderView } from "@/components/admin/admin-placeholder-view";

export default function AdminNotificationsPage() {
  return (
    <AdminPlaceholderView
      eyebrow="OWNER INBOX"
      title="NOTIFICATIONS"
      body="Filtered scoring alerts and dispute notifications are still pending, but the route is wired into the new admin panel shell and ready for the notification list implementation."
    />
  );
}
